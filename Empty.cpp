#include "Empty.h"

Empty::Empty(int _posX, int _posY) {
    //Building(_posX, _posY);//geht nicht mehr, da Building ja jetzt abstract. Klasse sowieso irrelevant
    label = 0;
    width = 1;
    length = 1;
}